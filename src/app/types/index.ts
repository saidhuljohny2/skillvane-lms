export interface LoggedInStudent {
  email: string;
  name: string;
  enrolledCourses: string[];
}

export interface StudentDetails {
  name: string;
  email: string;
  phone: string;
  password: string;
}
